version: "3.9"

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: dms_backend
    restart: unless-stopped
    ports:
      - "9090:9090"
    env_file:
      - .env
    extra_hosts:
      - "host.docker.internal:host-gateway"
    volumes:
      - "C:/Users/subhradip.majumder/Desktop/projects/projects/eisamay_dms/backend/uploads:/app/uploads"
